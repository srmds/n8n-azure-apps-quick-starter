# Common pipeline variables for n8n deployment
variables:
  # Environment specific
  location: 'westeurope'
  
  # Build variables
  buildConfiguration: 'Release'
  buildPlatform: 'Any CPU'

  # n8n Docker image
  n8nImage: 'docker.io/n8nio/n8n:latest'
